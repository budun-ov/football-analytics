# ============================================================
# Football Analytics — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# NOTE: After installing these packages, run the TVCalib setup:
#   python setup_tvcalib.py
#
# TVCalib itself is installed as a local editable package by
# setup_tvcalib.py; it does not appear here.
# ============================================================

# ── Core scientific stack ────────────────────────────────────
numpy>=1.23
pandas>=1.5
scipy>=1.9

# ── Computer vision ──────────────────────────────────────────
opencv-python>=4.7
Pillow>=9.0

# ── Deep learning ────────────────────────────────────────────
torch>=2.0
torchvision>=0.15
ultralytics>=8.0          # YOLO detection model

# ── Object tracking ──────────────────────────────────────────
supervision==0.26.0       # ByteTrack + helper annotators

# ── Team classification ──────────────────────────────────────
transformers>=4.35        # SigLIP (google/siglip-base-patch16-224)
scikit-learn>=1.2         # KMeans, PCA, normalize

# ── Calibration dependencies (TVCalib) ───────────────────────
kornia>=0.6.3
pytorch-lightning>=1.5
SoccerNet>=0.1.34         # SoccerPitch geometry

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.64
more-itertools>=9.0
matplotlib>=3.5           # Used by TVCalib visualisation
pyyaml>=6.0               # config.yaml parsing

# ── Optional: faster multiprocessing for segmentation post-processing
# (TVCalib uses multiprocessing.Pool — no additional packages needed)
